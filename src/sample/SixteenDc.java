package sample;

import java.util.ArrayList;
import java.util.HashMap;

public class SixteenDc {

	public static void main(String[] args) {

		String str1= "London";
		
		 str1.charAt(0); 
		 str1.compareTo(str1);
		 str1.concat(str1);
		 str1.contains(str1);
		 str1.endsWith(str1);
		 str1.equals(str1);
		 str1.hashCode();
		 str1.indexOf(0);
		 str1.length();
		 str1.repeat(1);
		 str1.replaceAll(str1, str1);
		 str1.split(str1);
		 str1.startsWith(str1);
		 str1.substring(0);
		 str1.toCharArray();
		 str1.toString();
		 
		 
		 HashMap hs = new HashMap<>();
		 
		 hs.clear();
		 hs.containsKey(hs);
		 hs.containsValue(hs);
		 hs.entrySet();
		 hs.equals(hs);
		 hs.get(hs);
		 hs.keySet();
		 hs.put(str1, hs);
		 hs.remove(hs);
		 hs.replace(str1, hs);
		 hs.size();
		 hs.values();
		 
		 ArrayList ls = new ArrayList<>();
		 ls.add(ls);
		 ls.clear();
		 ls.contains(ls);
		 ls.containsAll(ls);
		 ls.equals(ls);
		 ls.iterator();
		 ls.remove(0);
		 ls.retainAll(ls);
		 ls.set(0, ls);
		 ls.listIterator();
		 ls.stream();
		 ls.subList(0, 0);
		 
		 






	}




}
